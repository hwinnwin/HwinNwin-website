name = "hwinnwin-website"
compatibility_date = "2024-11-10"
pages_build_output_dir = "dist/public"

[build]
command = "npm install && npm run build"
cwd = ""

[build.upload]
format = "directory"
dir = "dist/public"

# Environment variables for build (if needed)
# [env.production]
# NODE_ENV = "production"
